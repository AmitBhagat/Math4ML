import * as mafs from "mafs";
console.log("Mafs exports:", Object.keys(mafs));
console.log("Plot keys:", Object.keys(mafs.Plot));
console.log("Line keys:", Object.keys(mafs.Line));
console.log("Theme keys:", Object.keys(mafs.Theme));
